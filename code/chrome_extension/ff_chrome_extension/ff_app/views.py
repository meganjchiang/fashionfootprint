from django.shortcuts import render

# Create your views here.
# FROM https://docs.djangoproject.com/en/5.0/topics/http/views/
# A view function, or view for short, is a Python function that takes a
# web request and returns a web response.
# This response can be the HTML contents of a web page, or a redirect,
# or a 404 error, or an XML document, or an image . . . or anything, really.
# The view itself contains whatever arbitrary logic is necessary to return
# that response.

# where get data functions should be